import React from 'react'
import ReactDOM from 'react-dom/client'
import ReceptionUI from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <ReceptionUI />
    </React.StrictMode>,
)